## Cherry
A toy renderer for fun.

### Usage
Default render (writes `binary.ppm`):

```bash
./Cherry
```

Override resolution / spp / output:

```bash
./Cherry --size 800x600 --spp 64 -o out.ppm
```

Switch integrator:

```bash
./Cherry --integrator normal
```
